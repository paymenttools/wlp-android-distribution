# These settings are synced to GitHub by https://probot.github.io/apps/settings/

_extends: .github

repository:
  # See https://docs.github.com/en/rest/reference/repos#update-a-repository for all available settings.
  # The name of the repository. Changing this will rename the repository
  name: wlp-android-distribution

  # Either `true` to make the repository private, or `false` to make it public.
  private: false

  # Note: `permission` is only valid on organization-owned repositories.
  # The permission to grant the collaborator. Can be one of:
  # * `pull` - can pull, but not push to or administer this repository.
  # * `push` - can pull and push, but not administer this repository.
  # * `admin` - can pull, push and administer this repository.
  # * `maintain` - Recommended for project managers who need to manage the repository without access to sensitive or destructive actions.
  # * `triage` - Recommended for contributors who need to proactively manage issues and pull requests without write access.

# See https://docs.github.com/en/rest/reference/teams#add-or-update-team-repository-permissions for available options
teams:
  - name: dev-team-moby
    permission: admin
  - name: infrastructure-administration
    permission: admin

collaborators:
  - username: alex-r-smartdata
    # Note: Only valid on organization-owned repositories.
    # The permission to grant the collaborator. Can be one of:
    # * `pull` - can pull, but not push to or administer this repository.
    # * `push` - can pull and push, but not administer this repository.
    # * `admin` - can pull, push and administer this repository.
    permission: admin
#  - username: adobresol
#    permission: admin

    # Reve devs
  - username: lehmansnDev
    permission: pull
  - username: superus8r
    permission: pull
  - username: rd-daniela-becker
    permission: pull
  - username: TheElementNrOne
    permission: pull
  - username: Faltenreich
    permission: pull
  - username: TilmanDo
    permission: pull
  - username: rd-henry-stecklum
    permission: pull
  - username: PatrickTheDev
    permission: pull
  - username: kknappe
    permission: pull
  - username: rd-stefan-jager
    permission: pull
  - username: lukaszkalnik
    permission: pull
  - username: rd-christian-dziuba
    permission: pull
  - username: Hackevent2025
    permission: pull
  - username: rd-gregor-hermanowski
    permission: pull
  - username: svenjacobs
    permission: pull
  - username: rd-inci-cagirici
    permission: pull
  - username: LadyLazorcorn
    permission: pull
